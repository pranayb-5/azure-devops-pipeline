# tools/myscript.py
import sys
# ...do work...
result = "42"  # compute your value
print(result)  # IMPORTANT: print only the value if you plan to capture stdout
sys.exit(0)    # non-zero will fail the step
